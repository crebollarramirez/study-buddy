"use client";

const logoutButton = () => {
    return (
        <div>
            <button
            className="ml-2 bg-blue-500 text-white p-2 rounded"
            >
            Logout
            </button>
        </div>
    );
};

export default logoutButton;